@HelixWOProposal_Append

  Feature: user validates WO rule isnt triggered if alarm is cleared before WO creation time
    Scenario: user validates WO rule isnt triggered if alarm is cleared before WO creation time

#      Given Helix sends create incident message to OW with tag wo_1 rule
#      When OW creates an OP ticket
#      Then work order should be created in 720 secs (not sure about WO link to OP)
#      When Helix sends append alarm message to OP with tag wo_38 rule
#      Then another work order should be created and linked OP
#      When Helix sends append alarm message to OP with tag wo_107
#      Then a new OP ticket should be created
#      And a new work order should be created and linked to new OP ticket